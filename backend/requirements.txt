# Required for Ultralytics' built-in object tracker (model.track(), used by
# backend/detector.py's detect_and_track() for Live Detection's one-event-
# per-physical-product logic). Ultralytics can auto-install this at runtime
# if it's missing, but that's unreliable — install it explicitly instead.
lapx>=0.5.2

# Required for the Inventory & Product Exit PDF report (backend/reports.py,
# GET /api/reports/inventory/pdf).
reportlab>=4.0.0
